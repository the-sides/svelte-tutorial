<script>
    // import myGame from './scripts/game.js'
    console.log('Hi from script!')
</script>

<h1>Syntax Features</h1>
<h2>Anything goes!</h2>
<p>Elements live in harmony</p>
<p>HTML Elements don't need a root</p>
<!-- HTML Attributes are vanilla (with caveats) -->
<p class="only-here">Scripts are still compiled, not run as is in browser</p>    
<p>Styles are scoped to the component/file</p>

<h3>I will get into the directory and file structure once we're looking at SvelteKit</h3>

<hr>
<h1>Variables</h1>
<p>Variables set in the {'<script>'} tag are available in HTML elements</p>
<p>Expressions within brackets:</p>
<p>2+2 = </p>

<style>
    * {
        margin: 2rem;
    }
    .only-here {
        font-size: 18px;
        font-family: monospace;
    }

    p:last-child {
        color: yellow;
    }
    
</style>
